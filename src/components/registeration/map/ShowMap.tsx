import React from "react";
import MapComponent from "./Map";

const ShowMap = () => {
  return (
    <div className=" w-[60%] h-screen flex items-center justify-center m-auto ">
      <MapComponent />
    </div>
  );
};

export default ShowMap;
